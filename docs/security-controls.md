# Security Controls

Controls are layered: backend authorization, strict DTO validation, cookie/CSRF
protection, bounded requests and rate limits, private storage, malware scanning,
envelope encryption, atomic counters, redacted structured logs, immutable audit
views, retention jobs, and production configuration guards. Failure of scanner,
authentication tag, authorization, or critical configuration fails closed.

