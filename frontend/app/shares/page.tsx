import Link from "next/link"; import { ShareList } from "@/components/share-list";
export default async function SharesPage({ searchParams }: { searchParams: Promise<{ workspace?: string }> }) { const { workspace } = await searchParams; return <main className="mx-auto min-h-screen max-w-5xl px-5 py-10"><div className="flex items-center justify-between"><Link href="/dashboard" className="text-blue-800 underline">← Dashboard</Link>{workspace && <Link className="button-primary" href={`/shares/new?workspace=${workspace}`}>Buat share</Link>}</div><h1 className="mb-8 mt-5 text-3xl font-bold">Share</h1>{workspace ? <ShareList workspaceId={workspace} /> : <p>Workspace belum dipilih.</p>}</main>; }
